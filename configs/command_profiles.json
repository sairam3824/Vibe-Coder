{
  "python": {
    "name": "python",
    "commands": ["pytest", "ruff check .", "python -m compileall ."],
    "description": "Default Python validation commands."
  },
  "node": {
    "name": "node",
    "commands": ["npm test -- --runInBand", "npm run lint", "npm run build"],
    "description": "Default Node validation commands."
  },
  "hybrid": {
    "name": "hybrid",
    "commands": ["pytest", "npm run build"],
    "description": "Simple mixed stack validation commands."
  }
}

